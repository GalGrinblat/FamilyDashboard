NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# REQUIRED for production: used by proxy.ts to verify authorized_users without a public-read policy
# Without this, the app falls back to a weaker public-read policy on the authorized_users table
SUPABASE_SERVICE_ROLE_KEY=
GEMINI_API_KEY=
